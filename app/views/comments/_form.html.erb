<%= form_for([@article, @article.comments.build]) do |f| %>
  <p>
    <%= f.label :'Ваше имя' %><br>
    <%= f.text_field :commenter %>
  </p>
  <p>
    <%= f.label :Комментарий %><br>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit :Отправить%>
  </p>
<% end %>